two things about the replication package:

Imagine I will be zipping the /Replication/ and send to a comittee

1. I have created a main.do --> put the macro settings and use include to call other dofiles... (so this should be a "main" dofile and that the comittee should only open this, change the macro path, and then just run smoothly)

2. the ipynb file stands alone. this is fine. 

3. create a README.txt talking about code structure & data in /raw/

