Time to prepare descriptives

Find the dofile called ASEC_Descriptives_3-30.do

here are the descriptives that you need to produce:

1. overall
    - kdensity of dv_rating_beta (i.e. kinda what proportion of workers distributes at each exposure score)
      NOTE: weight by person-level survey weights (or occ cell size) so density reflects 
      the worker distribution, not the occupation distribution --> reject: the data is already worker lvl
    - a simple tex table listing some of the most exposed occ and least exposed occ (as in Eloundou's csv)
      eg.      | Exposure Score | Most Exposed Occupations | Least Exposed Occupations
      (and perhaps list 5 of them) -- for this you may need the csv in raw/Eloundou/ in which variable title is the title of the occupation
      NOTE: consider adding N (CPS obs per occ) so readers can gauge sample coverage;
      spot-check SOC titles — some are cryptic, use recognizable labels  --> reject: no need. Just use the eloundou's csv. 3 columns, keep it simple

2. het
    - kdensity of dv_rating_beta, two lines one for college one for non-college (so as to compare the exposure to each group)
      NOTE: this is the most important descriptive for the het story — if distributions
      are similar, the asymmetric wage result is more striking

3. additional descriptives (suggested)
    - time-series plot: mean unemployment rate by high/low exposure (e.g. above/below median beta) 
      over 2018-2025, raw (no regression). The "eyeball" DiD before formal estimates.--> addition: do it for 25p 50p 75p... (split the sample into 4 groups that is) beta(vertical axis) and year 22 23 24 25 (horizontal axis)
    - sample summary table: N, mean age, % female, % college, mean wage — overall and by
      high/low exposure terciles. Standard for empirical papers. -> addition: yea but a similar thing: for 25p 50p 75p  (split the sample into 4 groups that is)